// @flow

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    field: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        width: 48 * 6
    },
    row: {
        flexDirection: 'row'
    }
})

export default styles
